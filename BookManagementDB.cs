using BookManagement.Model;

namespace BookManagement.DB
{
    static class ProjectDB
    {
        public static List<User> users = new() { };
        public static List<Book> books = new() { };
    }
}